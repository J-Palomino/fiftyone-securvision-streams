opencv-python>=4.5
numpy
requests
# Optional: ONVIF camera discovery (install both for auto-discovery)
onvif-zeep>=0.2.12
WSDiscovery>=2.0.0
# Optional: Object detection (install for analyze/detection features)
# torch>=1.9
# ultralytics>=8.0
